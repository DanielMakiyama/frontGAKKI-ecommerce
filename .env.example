# Opcional. O client.js já usa http://localhost:8080/api/v1 como padrão,
# então em desenvolvimento não é preciso criar um .env.
# Defina apenas para apontar para outro ambiente.
VITE_API_URL=http://localhost:8080/api/v1
